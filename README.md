# CoVLA reproduction

The data loader and visualization work live in `src/data.py` and
`src/visualize.py`.

The Week 2 model and training files are intentionally incomplete starter
shells. Implement the architecture in `src/model.py`, then implement batching,
training, evaluation, and logging in `src/train.py`.
